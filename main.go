package main

import (
	"fmt"
	// "golang_interview/leetcode"
)

func main() {
	// 测试大数
	mod := 10 ^ 9 + 7
	fmt.Println(mod)
	mod1 := 1000000007
	fmt.Println(mod1)

	/* // leetcode lru链表
	var lruCache leetcode.LRUCache
	lruCache = leetcode.Constructor(2)

	lruCache.Put(1, 1)
	lruCache.Put(2, 2)
	lruCache.Get(1)
	lruCache.Put(3, 3)
	lruCache.Get(2)
	lruCache.Put(4, 4)
	lruCache.Get(1)
	lruCache.Get(3)
	lruCache.Get(4) */
}
